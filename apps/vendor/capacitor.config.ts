import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.vendor',
  appName: 'Vendor',
  webDir: '../../dist/apps/vendor',
  bundledWebRuntime: false,
};

export default config;
